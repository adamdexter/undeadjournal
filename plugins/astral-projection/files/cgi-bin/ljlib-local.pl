#!/usr/bin/perl
# Local site additions, loaded automatically by ljlib.pl at startup.
# Installed by plugins/astral-projection/install.sh.
use LJ::AstralProjection;   # parallel-publish to the real deadjournal.com
1;
